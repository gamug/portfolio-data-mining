[pytest]
; "src" so every module's package (news_collector, extractor, pricing,
; sec_edgar) is importable the same way its own app.py imports it;
; "." so `apps.*` resolves from the repo root too. (portfolio_common is
; an installed dependency, not under src/.)
pythonpath = src .
testpaths = tests
asyncio_mode = auto
