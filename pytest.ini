[pytest]
; "src" so every module's package (news_collector, extractor, common,
; pricing, sec_edgar) is importable the same way its own app.py imports
; it; "." so `apps.*` resolves from the repo root too.
pythonpath = src .
testpaths = tests
asyncio_mode = auto
