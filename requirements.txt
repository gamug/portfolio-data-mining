# Merged dependency set for all five modules under src/ — one venv, one
# install. Grouped by which module(s) pull each package in; a package
# listed once still gets installed once (pip dedupes by name).
#
# torch is intentionally NOT listed here (news_nlp only) — install it
# separately with the wheel matching your CUDA version, same as the
# original news-nlp project:
#   .venv\Scripts\python.exe -m pip install torch --index-url https://download.pytorch.org/whl/cu124

# --- shared web/runtime -------------------------------------------------
httpx[http2]>=0.27.0
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-dotenv>=1.0.0
pandas>=2.0
numpy
tqdm>=4.66
requests

# --- news_collector (src/news_collector — discovery stage) --------------
ddgs>=9.0.0
yfinance>=0.2.40
tenacity>=8.3.0
aiosqlite>=0.20.0
feedparser>=6.0.11
lxml>=5.2.0
pyyaml>=6.0.1
rich>=13.7.1

# --- extractor (src/extractor — news-crawler, full-text extraction) -----
beautifulsoup4>=4.12
trafilatura>=1.12
langdetect>=1.0.9

# --- news_nlp (src/news_nlp — sentiment + NER) ---------------------------
transformers>=4.40
datasets>=2.19
accelerate>=0.30
evaluate>=0.4
seqeval>=1.2
huggingface_hub>=0.24

# --- pricing (src/pricing — Finnhub pricing/news/market data) -----------
finnhub-python==2.4.29

# --- sec_edgar (src/sec_edgar — SEC EDGAR filings) -----------------------
edgartools==5.44.1

# --- dev / test -----------------------------------------------------------
pytest>=8.2
pytest-asyncio>=0.24
hypothesis>=6.100
respx>=0.21
